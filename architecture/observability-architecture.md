# Observability Architecture

Epic 0 includes correlation ID middleware, health checks, Problem Details readiness, and structured logging readiness.

Required telemetry fields for future epics include timestamp, service name, level, correlation ID, event name, entity type, entity ID, safe patient ID where applicable, and exception details.

Application Insights and Log Analytics are the Azure monitoring targets.
