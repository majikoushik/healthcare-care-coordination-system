# DevOps Guide

CI is defined in `.github/workflows/ci.yml` and restores/builds/tests backend and frontend foundations.

Future DevOps work will add image build validation, Azure deployment templates, environment promotion, and release gates.
